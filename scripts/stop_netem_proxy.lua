local script=debug.getinfo(1,'S').source:sub(2);local dir=script:match('^(.*)[/\\]');package.path=dir..'/?.lua;'..package.path
local b=require('_bootstrap');local o=b.lib('args').parse(arg);local process=b.lib('process');local json=b.lib('json');local udp=b.lib('udp');local pid=b.lib('args').integer(o.pid or o['process-id'],'pid',1);local exe=assert(o.exe or o['expected-exe'],'--exe required');if not process.inspect(pid)then return end;process.assert_identity(pid,exe);udp.send(o['control-addr']or'127.0.0.1:63200',json.encode({version=1,action='shutdown'}));if not process.wait(pid,7000)then process.stop(pid,exe)end
