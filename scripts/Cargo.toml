[workspace]
resolver = "2"
members = [
    "script-abi",
    "base_content",
]

# 針對 DLL 腳本調整的發布設定檔 — 最佳化但保留偵錯符號
# 被剝離，以便 .dll 保持精簡。
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
debug = false
strip = "symbols"
