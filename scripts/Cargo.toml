[workspace]
resolver = "2"
members = [
    "script-abi",
    "base_content",
]

# 針對 DLL 腳本調整的發布設定檔：最佳化且不輸出 PDB / debug info。
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
debug = "none"
split-debuginfo = "off"
strip = "symbols"

[profile.release.build-override]
debug = "none"
